name: Docker Series Builds

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: docker build python
      run: |
        docker build . -t hello-api/wiredcraft:1.0.0
    - name: docker tag
      run: |
        docker tag hello-api/wiredcraft:1.0.0 server.jahanxb.xyz/hello_api:v4.0
    - name: docker push image
      run: |
        docker push server.jahanxb.xyz/hello_api:v4.0
#        echo "need to push to inseas cuse  "
         
         
